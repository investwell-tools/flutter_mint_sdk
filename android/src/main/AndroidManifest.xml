<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The Mint SDK performs SSO/allowed-modules network calls. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
